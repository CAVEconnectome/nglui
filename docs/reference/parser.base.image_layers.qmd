# parser.base.image_layers { #nglui.parser.base.image_layers }

`parser.base.image_layers(state)`

Get all image layer names in the state

## Parameters

| Name    | Type   | Description                       | Default    |
|---------|--------|-----------------------------------|------------|
| `state` | dict   | Neuroglancer state as a JSON dict | _required_ |

## Returns

| Type   | Description         |
|--------|---------------------|
| list   | List of layer names |